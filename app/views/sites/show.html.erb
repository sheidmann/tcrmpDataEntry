<div class="main">
  <div class='container'>
    <p><%= button_to('Field Progress', "/sites", :method => "get") %></p>
    <h1><%= "#{@site.site_name} (#{@site.site_code})"%></h1>
    <h2>Surveys</h2>
    <table id="siteSurveyList", class="display table table-striped" style="width: auto">
      <thead>
        <% #print table header %>
        <tr>
          <th>Survey Type</th>
          <th>Transect Number</th>
          <th>Observer</th>
          <th>Date</th>
        </tr>
      </thead>
      <tbody>
        <% @site_surveys.each do |ss|  %>
          <tr>
            <td><%= SurveyType.find(ss.survey_type_id).type_name %></td>
            <td><%= ss.rep %></td>
            <td><%= User.find(ss.user_id).name %></td>
            <td><%= ss.boatlog.date_completed %></td>
          </tr>   
        <% end %>
      </tbody>
    </table>
    </div>
</div>